import React from 'react';
import PropTypes from 'prop-types';
import { Text } from 'react-native';

const RecoverSMS = ({ navigation }) => {
    return (
        <Text>Recover SMS</Text>
    );
};

RecoverSMS.propTypes = {

};

export default RecoverSMS;